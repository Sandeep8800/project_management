package com.nexuspms.governance.domain;

/** PRD FR-6; HLD S4: SCRUM/KANBAN show one board type, HYBRID shows both over the same shared backlog. */
public enum ProjectMethodology {
    SCRUM,
    KANBAN,
    HYBRID
}
